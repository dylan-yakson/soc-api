# Friendly reminder...
# This is for testing purposes ONLY
# The developers (Dylan.. lol) accept no responsibility for any misuse YOU may cause with this.
import random
import time
import re
import string
import secrets
import os
import urllib.request
import json
import time
import csv
import logging, sys, getopt
import array
from datetime import date, datetime
from multiprocessing import Process
import asyncio

class Proxy_Engine():
    def __init__(self, proxy_site_api_key):
        self.proxy_site_api_key = proxy_site_api_key
            
    async def pull_proxy(self):
        return 'test'

    async def gen_proxies(self):
        return 'test'

    async def refresh_proxy_list(self):
        return 'test'
